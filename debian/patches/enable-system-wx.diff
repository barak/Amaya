Use the system wxWidgets instead of building it and linking statically
Merged upstream

Index: amaya-9.51/Amaya/thotlib/base/AmayaApp.cpp
===================================================================
--- amaya-9.51.orig/Amaya/thotlib/base/AmayaApp.cpp	2006-04-10 15:13:16.000000000 +0100
+++ amaya-9.51/Amaya/thotlib/base/AmayaApp.cpp	2006-05-21 14:55:35.586232960 +0100
@@ -76,7 +76,9 @@
   WX_GL_MIN_ALPHA, 0, /* don't change the position of this entry (8) */
   WX_GL_STENCIL_SIZE, 1,
   WX_GL_DOUBLEBUFFER,
+#ifdef _WINDOWS
   WX_GL_NOT_ACCELERATED, /* don't change the position of this entry (12) */
+#endif /* _WINDOWS */
   0
 };
 #endif /* _GL */
@@ -385,6 +387,7 @@
   -----------------------------------------------------------------------*/
 int * AmayaApp::GetGL_AttrList()
 {
+#ifdef _WINDOWS
   // depending on thot.rc option set/unset OpenGL hardware acceleration
 
   // default is no acceleration because of strange bugs on
@@ -397,6 +400,7 @@
     AttrList[12] = 0; /* ok enable opengl hardware acceleration */
   else
     AttrList[12] = WX_GL_NOT_ACCELERATED; /* disable opengl hardware acceleration */
+#endif /* _WINDOWS */
   return AttrList;
 }
 #endif /* _GL */
Index: amaya-9.51/Amaya/Makefile.in
===================================================================
--- amaya-9.51.orig/Amaya/Makefile.in	2006-05-21 14:55:35.537240408 +0100
+++ amaya-9.51/Amaya/Makefile.in	2006-05-21 15:17:58.312107600 +0100
@@ -28,7 +28,7 @@
 
 srcdir	= $(THOTDIR)
 
-default : tools gl wxwidgets thotlib batch amaya_prog @PRINTIFNOTGL@
+default : tools gl @BUILDWXWIDGETS@ thotlib batch amaya_prog @PRINTIFNOTGL@
 
 amaya : amaya_prog tools thotlib @PRINTIFNOTGL@
 
Index: amaya-9.51/Amaya/configure.in
===================================================================
--- amaya-9.51.orig/Amaya/configure.in	2006-05-21 14:55:35.555237672 +0100
+++ amaya-9.51/Amaya/configure.in	2006-05-21 15:19:23.126213896 +0100
@@ -243,6 +243,7 @@
     with_gtk="no"
   fi
 fi
+AC_ARG_ENABLE(system-wx,       [  --enable-system-wx try the wxWidgets system library ])
 
 if test "$with_gtk" = "" -o "$with_wx=" = "" ; then
   # Nothing is enable so force the default GUI toolkit (WX)
@@ -420,6 +421,15 @@
 fi
 
 if test "$with_wx" = "yes" ; then
+  if test "$enable_system_wx" = "yes"; then
+    AC_PATH_PROG(WXCONFIG, wx-config)
+    if test -z "$WXCONFIG"; then
+      enable_system_wx="no"
+    fi
+  fi
+
+  if test "$enable_system_wx" != "yes"; then
+
   # WX_SRCDIR is better to understand the code than with_wxsrcdir variable
   if test ! -f $with_wxsrcdir/Makefile.in ; then
     $with_wxsrcdir = ../../wxWidgets
@@ -511,7 +521,14 @@
 
   # setup include paths  
   # "-I$WX_SRCDIR/src/png -I$WX_SRCDIR/src/jpeg -I$WX_SRCDIR/src/tiff" is a temporary patch to fix incorrect generated paths by wx-config --cxxflags (remove it when wxWidgets will be fixed)
-  WX_INCLUDES="-I$WX_SRCDIR/src/png -I$WX_SRCDIR/src/jpeg -I$WX_SRCDIR/src/tiff `$WXCONFIG --cxxflags`"
+  WX_INCLUDES="-I$WX_SRCDIR/src/png -I$WX_SRCDIR/src/jpeg -I$WX_SRCDIR/src/tiff"
+
+  BUILDWXWIDGETS="wxwidgets"
+  AC_SUBST(BUILDWXWIDGETS)
+
+ fi
+
+  WX_INCLUDES="$WX_INCLUDES `$WXCONFIG --cxxflags`"
   AC_SUBST(WX_INCLUDES)
 
   # setup libraries paths
@@ -1502,7 +1519,7 @@
        EXTRA_MAKEFILE_IN="$srcdir/davlib/Makefile.in"
     fi
 
-    if test "$with_wx" = "yes" ; then
+    if test "$with_wx" = "yes" -a "$enable_system_wx" != "yes" ; then
        EXTRA_MAKEFILE_IN="$EXTRA_MAKEFILE_IN $WX_SRCDIR/Makefile.in $WX_SRCDIR/configure.in"
     fi
 
@@ -1611,7 +1628,11 @@
   if test "$with_wxdebug" = "yes" ; then
     echo "Amaya configured with wxWidgets {srcdir=$WX_SRCDIR} (debug)"
   else
-    echo "Amaya configured with wxWidgets {srcdir=$WX_SRCDIR}"
+    if test "$enable_system_wx" != "yes" ; then
+        echo "Amaya configured with wxWidgets {srcdir=$WX_SRCDIR}"
+    else
+        echo "Amaya configured with wxWidgets (system)"
+    fi
   fi
 fi
 
